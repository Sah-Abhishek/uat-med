videos/
docs/

# Generated knowledge-graph outputs (graphify) — rebuildable, don't version.
graphify-out/

# Local docker-compose overrides (may hold secrets) — keep the example only.
.env
!.env.docker.example
